<div class ="container">

  <div class="text mt-5">
    <h2 class="site-title mb-3">お酒一覧</h2>
    <p>※商品画像をクリックすると、詳細をご覧いただけます。</p>
  </div>
    <ul class="flex-item">
      <% @liquors.each do |liquor| %>
      <li>
        <%= link_to liquor_path(liquor) do %>
          <div class="item">
            <%= attachment_image_tag liquor, :image %>
              <div class="mask">
              </div>
          </div>
          <h3 class="mt-3 mb-1"><%= liquor.name %></h3>
          <p>¥<%= (liquor.detail) %></p>
        <% end %>
      </li>
     <% end %>
    </ul>

    <div class="paginate">
      <%= paginate @liquors %>
    </div>
</div>